import 'package:flutter/material.dart';

class AppColors {
  // Colores principales del diseño (desde Figma/diseño)
  static const Color primary = Color(0xFF6366F1); // Morado/Azul principal
  static const Color background = Color(0xFFFAFAFA); // Fondo blanco
  static const Color accent = Color(0xFFF59E0B); // Color de acento (ej. para FAB)
  static const Color textDark = Color(0xFF374151); // Texto oscuro
  static const Color textLight = Color(0xFF9CA3AF); // Texto claro
  static const Color surface = Color(0xFFF3F4F6); // Superficie gris claro
  static const Color darkBackground = Color(0xFF000000); // Fondo negro
  
  // Colores de equipos
  static const Color homeTeam = Color(0xFFE63946); // Rojo para equipo local
  static const Color awayTeam = Color(0xFFF59E0B); // Naranja para equipo visitante
  
  // Colores de eventos
  static const Color goal = Color(0xFFE63946); // Rojo para goles
  static const Color redCard = Color(0xFFE63946); // Rojo para tarjeta roja
  static const Color yellowCard = Color(0xFFF59E0B); // Amarillo para tarjeta amarilla
  static const Color substitution = Color(0xFF292D32); // Gris oscuro para sustitución
  static const Color interruption = Color(0xFF9CA3AF); // Gris para interrupciones
  
  // Colores de controles
  static const Color playButton = Color(0xFF6366F1);
  static const Color pauseButton = Color(0xFF6366F1);
  static const Color stopButton = Color(0xFF6366F1);
  
  // Otros
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color gray = Color(0xFF9CA3AF);
}
